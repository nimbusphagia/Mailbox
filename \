import { Button } from "./button";
import { Modal } from "./modal";
import { ToggleGroup, ToggleGroupItem } from "./toggle-group";

type Props = {
  hideFn: () => void,
}
export function NewMessageModal({ hideFn }: Props) {
  return (
    <Modal>
      <div className="flex-1 max-w-[35%] h-[clamp(40%,50%,600px)  bg-fg4 p-2">
        <div className="flex items-center justify-between ">
          <Button className="text-bg3" onClick={hideFn}>cancel</Button>
          <Button className="text-blue-dark/40">new message</Button>
        </div>
        <div>
          <ToggleGroup
            variant="outline"
            type="single"
            className="rounded-none outline-bg4"
            defaultValue="contacts"
          >
            <ToggleGroupItem
              value="contacts"
              aria-label="filter by contacts"
              className="rounded-none"
            >Contacts</ToggleGroupItem>

            <ToggleGroupItem
              value="explore"
              aria-label="show all users"
              className="first:rounded-none last:rounded-none"
            >explore</ToggleGroupItem>
          </ToggleGroup>
          <div>

          </div>
        </div>
      </div>
    </Modal>
  )
}
